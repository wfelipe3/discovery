prefix_rs ="{{ prefix_rs }}"
location ="{{ location }}"
channel_g="{{ channel_g }}"

credential {
    subscription_id = "{{ subscription_id }}"
    client_id = "{{ client_id }}"
    client_secret = "{{ client_secret }}"
    tenant_id = "{{ tenant_id }}"
    object_id = "{{ object_id }}"
}
